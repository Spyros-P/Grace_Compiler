fun main () : nothing

   var r : char[20];

   fun reverse (ref s : char[]) : nothing
      var i, l : int;
   {
      l <- 17;
      i <- 0;
      while i < l do {
         r[i] <- s[2];
         i <- i+1;
         writeChar(s[2]);
         writeInteger(i);
         writeChar('\t');
         writeInteger(l);
         writeChar('\n');
      }
      r[i] <- '\0';

   }

{ $ main
   reverse("\n!dlrow olleHeee");
} $ main

