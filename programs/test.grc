var N,x1,x2,t,i : int;
{
    N <- 10;
    
    i <- 0;
    x1 <- 0;
    x2 <- 1;

    while (i<=N) do {
        writeInteger(x1);
        t <- x2;
        x2 <- x1+x2;
        x1 <- t;

        i <- i+1;
    }
}
