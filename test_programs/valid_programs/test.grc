fun main() : nothing

    fun inc(ref y : int) : nothing
    {
        y <- y+1;
    }

    var x   : int;

{
    x <- 0;
    writeString("x = ");
    writeInteger(x);
    writeString(" (before inc)\n");
    inc(x);
    writeString("x = ");
    writeInteger(x);
    writeString(" (after inc)\n");
}