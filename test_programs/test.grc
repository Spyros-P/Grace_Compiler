fun main() : int

    var tmp,x,y,N : int;

{
    $ Calculate the N first fibonacci numbers
    N <- 40;

    x <- 0;
    y <- 1;

    while (N > 0) do {
        tmp <- x + y;
        x <- y;
        y <- tmp;

        N <- N - 1;

        writeInteger(x);
    }
    
    return 0;
}

