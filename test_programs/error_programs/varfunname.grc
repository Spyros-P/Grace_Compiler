$$
    This program fails because a variable is
    declared that has the same name as a function.
    Some kind of identifier error should be produced.
$$
fun varfunname(): nothing
    fun foo(): int
    {
        return 5;
    }
    var foo: int;
{
   foo <- foo();
}
