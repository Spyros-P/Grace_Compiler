$$
    This program should fail because grace does not
    support assignment between arrays.
    Some kind of type error should be triggered.
$$

fun main() : nothing

    var x : int[10];
    var y : int[10];

{
    x <- y;
}
