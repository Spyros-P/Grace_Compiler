$$
    This program fails because it calls
    the 'test' function with the wrong
    number of parameters.
$$
fun numarg () : nothing
   fun test(a, b, c: int) : int
   {
      return a + b + c;
   }
   var a, b, c, d: int;
{
   d <- test(a, b, c, d);
}
